# Deprecated Python Folder

This `Python/` folder contains an older Tkinter-based IDE implementation and support modules kept for historical reference. The actively maintained Python implementation lives under `Time_Warp_Python/`.

Please prefer:

- `Time_Warp_Python/` for the current Python IDE and tests
- `legacy/` for additional archived experiments

This folder will be archived or relocated in a future cleanup pass once all references are updated.
