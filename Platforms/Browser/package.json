{
  "name": "time-warp-web",
  "version": "4.0.0",
  "private": true,
  "license": "MIT",
  "description": "Time Warp IDE - Web implementation (static HTML/JS)",
  "scripts": {
    "serve": "python3 -m http.server 8000",
    "serve:port": "PORT=${PORT:-8000} python3 -m http.server $PORT"
  }
}
